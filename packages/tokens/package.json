{
  "name": "@wds/tokens",
  "version": "0.0.0",
  "private": true,
  "description": "WDS design tokens — 2-tier architecture (primitive → alias). Components import from this package only via the alias layer.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./scale.css": "./src/scale.css",
    "./reset.css": "./src/reset.css"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
